{
  "name": "recipe-api",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch src/index.ts",
    "db:init": "bun scripts/db-init.ts",
    "db:build": "bun scripts/db-build.ts",
    "db:seed": "bun scripts/db-seed.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.5"
  },
  "dependencies": {
    "postgres": "^3.4.3",
    "zod": "^4.3.6"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
